{
  "name": "@lawallet-nwc/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.6.1",
    "@asteasolutions/zod-to-openapi": "^9.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
