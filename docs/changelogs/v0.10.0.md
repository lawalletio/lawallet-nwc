# v0.10.0

## Release date

2026-04-30

## Summary

The biggest release since the project was OpenSats-funded. Ships the full
monorepo, the complete admin dashboard, the user-facing Wallet, an
end-to-end Activity Log, dynamic branding, and the new
`LightningAddress 1→N` + `NWCConnection` schema that unlocks per-address
NWC routing.

## Highlights

### Monorepo migration (PR #187, #188)
- Moved to **pnpm workspaces + Turborepo** (Node 22.14.0)
- New layout: `apps/web`, `apps/docs` (Fumadocs), `apps/listener`, `packages/sdk`, `packages/shared`
- Removed the vendored proxy app — NWC Proxy is now an **external** service per `docs/services/NWC-PROXY.md`
- `@/*` alias rooted at `apps/web/`
- CI reworked for monorepo install + JWT_SECRET in build env

### Admin Dashboard E2E (PR #190)
- **Cards & Designs:** full CRUD, BoltCard QR, NFC scan flow, real API data via JWT
- **Users:** list with Nostr avatars, detail pages, social-media-style header with editable kind-0 profile, avatar/cover crop + Blossom upload with live progress, role picker with hierarchy-aware guard
- **Addresses:** wallet-style list + edit pages, primary toggle, optimistic updates, alias badges, `All users` admin toggle
- **Settings:** full persistence (every form input → DB), multi relays/blossom/smtp, validation, reset, Branding/Wallet/Infrastructure tabs
- **Identity circles:** community + avatar badge on home, linked to user/community pages
- **Maintenance mode:** honors DB flag, gates dashboard, JWT admin bypass
- **Dashboard:** EndpointError banner + ForwardingCard for IDLE/ALIAS modes, real-time NWC balance and status (SSE), responsive stat cards

### User-facing Wallet (PR #191)
- New `apps/web/app/(wallet)/` route group with mobile shell
- **NWC client facade** with offline cache and persistent sign-in
- Onboarding, Home (redesigned), Send, Receive, Scan flows
- Currency switcher, infinite-scroll Activity, WebLN integration
- Animated paid-flow with in-dialog UX
- Unit coverage for parser, contacts, and keypad

### Onboarding (PR #192)
- Confirm-root step + community auto-import
- Setup-now CTAs and dev reset
- Public `/register` route + admin bypass toggle
- Paid registration (Lightning payment required for new addresses)
- Real domain verification + endpoint URL field in setup wizard
- Drop `/register` page, route claim through admin

### Activity Log (system-wide)
- New DB model + permission + SSE event type
- Dual-sink helper, API endpoint, error-handler integration
- Instrumented USER, ADDRESS, NWC, INVOICE, CARD, SERVER emit sites
- Admin UI with filters, pagination, live updates, details dialog

### Branding & Theming
- Blossom-backed logo uploads, global logo, admin-only settings
- Theme tokens persisted globally via `/api/settings`
- New `BrandLogotype` component (with skeleton) — 6 consumers migrated
- Brand-icon WhatsApp/Discord/Nostr ostrich (currentColor)

### API & Schema
- **Schema:** `LightningAddress` 1→N + new `NWCConnection` model
- **Wallet API:** per-user addresses + NWC connection management
- **LUD-16:** mode-aware resolution (IDLE reject, ALIAS proxy, NWC route)
- **LUD-12:** payer comments on LUD-16 callback
- **LUD-21:** verify endpoint for LUD-16 invoices
- **Per-address invoices feed**, back-fill NWC connection coverage
- `effectiveConnectionString` on detail responses
- `users/me` exposes `effectiveNwcString`, `mode`, `username`, `redirect`
- SSE: broadcast `invoices:updated` on new invoice + PAID transition
- `useApi` SWR cache with logout teardown + invoices SSE routing

### Tooling & DX
- 22 new Claude Code agent skills (PR #189): next, prisma, tailwind, vitest, vite, typescript, turborepo, shadcn, vercel, deploy, accessibility, seo, etc.
- Comprehensive `ARCHITECTURE.md` with modules, boundaries, data flows
- Refreshed `CLAUDE.md`
- App favicon
- Theme-aware README logo

### Fixes
- Bunker QR login: invalid payload length on NIP-04 signers
- `lightning-addresses` endpoints accept JWT (not only NIP-98)
- NWC: toast on relay timeout, suppress SDK console spam
- React Compiler strict rules downgraded to warnings
- Vercel monorepo install path
- CI: pnpm version conflict, JWT_SECRET, env vars through turbo
- Dependency vulnerabilities, audit cleanup
- `min-w-0` on SidebarInset to prevent mobile overflow
- Topbar mobile fluid font sizing
- API users/[userId]: allow self-access regardless of USERS_READ
- Settings: expose `social_*` handles in the public subset

## Included PRs

- [#187](https://github.com/lawalletio/lawallet-nwc/pull/187) — pnpm + turborepo monorepo
- [#188](https://github.com/lawalletio/lawallet-nwc/pull/188) — remove proxy app, package stubs
- [#189](https://github.com/lawalletio/lawallet-nwc/pull/189) — Claude Code agent skills
- [#190](https://github.com/lawalletio/lawallet-nwc/pull/190) — admin dashboard E2E
- [#191](https://github.com/lawalletio/lawallet-nwc/pull/191) — user-facing wallet
- [#192](https://github.com/lawalletio/lawallet-nwc/pull/192) — full first-run onboarding flow

## Upgrade notes

- **Node:** ensure `22.14.0` (`.nvmrc`)
- **Install:** root install only — `pnpm install` (no more npm in `apps/web/`)
- **Schema migration:** run `pnpm --filter @lawallet-nwc/web exec prisma migrate deploy` — adds `NWCConnection` and `LightningAddress` 1→N changes
- **Env:** `JWT_SECRET` is now required in build env, not just runtime
- **NWC Proxy:** no longer vendored; provision externally per `docs/services/NWC-PROXY.md`
- **Routes:** `/wallet/*` is live; `/admin` remains the admin entrypoint; `/` still redirects to `NEXT_PUBLIC_LAWALLET_LANDING_URL`
