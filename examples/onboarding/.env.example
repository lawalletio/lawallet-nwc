# Public origin of YOUR LaWallet instance — the SDK signs NIP-98 events
# against this exact URL, so it must be the address the instance is
# publicly reachable at (no trailing slash).
VITE_LAWALLET_ENDPOINT=https://your-instance.example.com
