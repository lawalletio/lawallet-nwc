# Optional: Google Tag ID for the docs site analytics. Use the same ID as
# the one configured in the web admin Settings → Infrastructure tab so all
# surfaces report to the same property. Leave empty to disable.
# Examples: G-XXXXXXXXXX, GT-XXXXXXX, AW-XXXXXXXXX
NEXT_PUBLIC_GTAG_ID=
