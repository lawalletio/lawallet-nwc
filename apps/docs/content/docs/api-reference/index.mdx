---
title: API Reference
description: Live OpenAPI 3.1 reference for the LaWallet NWC REST API.
---

import { getOpenApiDocument } from '@lawallet-nwc/openapi'

export const spec = getOpenApiDocument()

The reference below is generated from the same `@lawallet-nwc/openapi` package the [`apps/web`](https://github.com/lawalletio/lawallet-nwc/tree/main/apps/web) instance serves at [`/api/openapi.json`](http://localhost:3000/api/openapi.json). Schemas are derived from the project's Zod definitions, so this page stays in lockstep with the running code.

<ApiReference spec={spec} />
