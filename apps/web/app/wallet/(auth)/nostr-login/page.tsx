import { NostrLoginChoice } from '@/components/wallet/onboarding/nostr-login-choice'

export const metadata = {
  title: 'Nostr login - LaWallet',
}

export default function NostrLoginPage() {
  return <NostrLoginChoice />
}
