import { NostrLoginMethods } from '@/components/wallet/onboarding/nostr-login-methods'

export const metadata = {
  title: 'Continue with Nostr - LaWallet'
}

export default function WalletNostrLoginPage() {
  return <NostrLoginMethods />
}
