import { SettingsScreen } from '@/components/wallet/settings/settings-screen'

export const metadata = { title: 'Settings - LaWallet' }

export default function SettingsPage() {
  return <SettingsScreen />
}
