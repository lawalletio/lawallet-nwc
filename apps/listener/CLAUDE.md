# apps/listener — NWC Payment Listener (stub)

Not yet implemented: `src/index.ts` is a stub and the package scripts are echo
warnings. The service will monitor NWC relays and dispatch LUD-22 webhooks.

Design doc: `docs/services/NWC-LISTENER.md`. Don't add real behavior here
without aligning with that spec; don't "fix" the echo scripts — they are
intentional placeholders until the service is scheduled on the roadmap.
